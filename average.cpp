#include <iostream>
using namespace std;

int main() {

    int a,b,c;
    double average;

    cout << " enter three number: ";
    cin >> a >> b >> c;
    average = (a + b + c) / 3.0;
    cout << "average is: " << average << endl;
}